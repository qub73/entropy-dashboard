# Status audit — 2026-04-21 (read-only)

**Audit purpose:** sanity-check the state left behind by the sprint v1.5
close-out and revised-6b preparation before we plan the next move. No code
changes, no commits, no pushes.

All numbers below come from replays run locally today. Replay script:
`algo/diagnostics/status_2026_04_21_replay.py` (read-only helper; not
committed).

---

## Part A — Shadow expectation regeneration integrity

### A1. Current `state/shadow_expectation.json` — config used

Metadata block:

```json
{
  "cell_name": "timeout_trail+E3+5x (revised 6b, no F3c)",
  "f3c_enabled": false,
  "generated_at": "2026-04-20T17:58:24.585045+00:00",
  "data_source": "Feb 18 - Apr 7 2026 Pi Kraken Futures ETH L2",
  "n_trades_total": 44
}
```

The file does NOT store a full config hash. The flags used (from
`algo/diagnostics/shadow_expectation_generator.py:simulate_promoted`):

| Flag | Value |
|---|---|
| F3c entry gate | **OFF** |
| timeout_trail | ON (post_signal_trail width=20 / floor=-60 / max_wait=30) |
| E3 time-decayed SL | ON (tighten to -25 at 60 bars if peak < +50) |
| tp_trail | ON (activates at peak ≥ +150 bps, trail width 50 bps) |
| Leverage | 5× (pnl_bps output is leverage-invariant; bucket stats are
  bps, not USD) |
| SL / TP | 50 / 200 (Pi live defaults) |
| knife / ext | 50 / 100 (Pi live defaults) |
| H_thresh | 0.4352 (unchanged) |
| dH<0 gate | ON (required for entry — from I1 filter) |

Reproduce: `python algo/diagnostics/shadow_expectation_generator.py --no-f3c`

### A2. Pi 21-day baseline replay, revised 6b, 5× leverage

Substrate: Feb 18 – Apr 7 2026 Pi Kraken Futures ETH L2 (21.3 days). Params:
Pi live defaults. F3c OFF. timeout_trail + E3 ON. 5× leverage.

| Metric | Value |
|---|---|
| n_trades | 44 (long 16 / short 28) |
| win_rate | 54.54% |
| compound_ret_pct | **+178.36%** |
| max_dd | **8.09%** |
| sharpe_daily | 14.51 |
| profit_factor | 3.69 |
| knife_rate | 22.73% |
| knife_catchers | 10 |

**DD measurement method:** max peak-to-trough on per-bar compound equity
curve, where equity is marked-to-market every bar via open-trade floating
PnL (not just at trade-close events). This is the same method
`phase4_sizing_sim.run_cell` uses.

#### Cross-check against Phase 4 `lev5x_ewoff_e3on` IS (F3c **ON**)

From `algo/reports/phase4_sizing_sim.json` Pi IS (the cell you referenced
with "+61.4% / 12.5% DD"):

| Metric | F3c ON (phase 4) | F3c OFF (today) | Delta |
|---|---|---|---|
| trades | 27 | 44 | **+17** |
| win_rate | 48.15% | 54.55% | +6.40pp |
| compound_ret_pct | +61.39% | +178.36% | **+116.97pp** |
| max_dd | 12.48% | 8.09% | **−4.39pp** |
| sharpe_daily | 11.94 | 14.51 | +2.57 |
| pf | 3.03 | 3.69 | +0.66 |
| knife_rate | 29.63% | 22.73% | **−6.90pp** |

**Note on your cited Sharpe 4.6:** that looks like the Kaggle-OOS Sharpe
(4.61), not Pi-IS. Pi-IS Sharpe was 11.94 in the phase 4 JSON. I've shown
the Pi-IS number above so the comparison is apples-to-apples.

**Interpretation:** on Pi IS, removing F3c strictly improves every metric.
This is retrospectively consistent with the sprint v1.5 Kraken-native
finding — and raises a concern we did NOT surface at the time: F3c may
have been net-negative on Pi IS too. The 29 trades F3c blocked on Pi IS
(27 → 44 delta) had, on average, enough upside to outweigh the gated
losers. That's the opposite of what the phase 2 ablation implied.

### A3. Kaggle 60d OOS replay, revised 6b, 5× leverage

Substrate: Kaggle ETH/USDT, last 60 days of the 2023-10-07 → 2024-10-09
archive. Params: v2 refit (SL=50, TP=150, knife=100, ext=None from
`algo/reports/phase2_kaggle_refit.json`). F3c OFF. timeout_trail + E3 ON.
5× leverage.

| Metric | Value |
|---|---|
| n_trades | 131 (long 58 / short 73) |
| win_rate | 32.82% |
| compound_ret_pct | **+26.03%** |
| max_dd | **38.61%** |
| sharpe_daily | 1.75 |
| profit_factor | 1.36 |
| knife_rate | 34.35% |

#### Cross-check against Phase 4 `lev5x_ewoff_e3on` Kaggle OOS (F3c **ON**)

From `algo/reports/phase4_sizing_sim.json` Kaggle OOS:

| Metric | F3c ON (phase 4) | F3c OFF (today) | Delta |
|---|---|---|---|
| trades | 76 | 131 | **+55** |
| win_rate | 40.79% | 32.82% | −7.97pp |
| compound_ret_pct | +102.62% | +26.03% | **−76.59pp** |
| max_dd | 14.84% | 38.61% | **+23.77pp** |
| sharpe_daily | 4.61 | 1.75 | −2.86 |
| pf | 2.06 | 1.36 | −0.70 |
| knife_rate | 26.32% | 34.35% | **+8.03pp** |

**Interpretation:** on Kaggle OOS, removing F3c strictly HURTS every
metric. The 55 extra trades F3c would have blocked here are net losers,
not winners. This is the opposite of the Pi-IS finding in A2.

**Reconciling A2 vs A3:** the substrates disagree on F3c's sign. F3c
hurts on Pi (Kraken Futures) IS and helps on Kaggle (Binance spot) OOS.
Since the ultimate deployment target is Kraken Futures, the Pi-IS result
is the relevant one — and it says we were right to drop F3c.

### A4. 1× leverage sanity on Kaggle (Pi DD ≤20%, skipped)

Pi 5x DD was 8.09% (<20%), no sanity check needed.

Kaggle 5x DD was 38.61% (>20%), triggered 1× replay:

| Metric | 5× | 1× |
|---|---|---|
| trades | 131 | 131 |
| compound_ret_pct | +26.03% | +6.47% |
| max_dd | 38.61% | **9.02%** |
| pf | 1.36 | 1.36 |
| knife_rate | 34.35% | 34.35% |

**1× DD is 9.02% — above the 8% threshold you flagged.** That means the
Kaggle DD at 5× is NOT a pure leverage artifact; the path-of-trades has
real drawdown. At 5×, the drawdown compounds roughly 4.3× (9.02 → 38.61),
which is within the expected envelope for compounding leveraged losses
(not exactly 5× because compounding asymmetry on drawdowns).

This is an honest adverse signal for Kaggle OOS: even without leverage,
the revised config accumulates a ~9% drawdown on Kaggle 60d. Drops to
~4% if we had F3c on. The Kaggle substrate strictly prefers F3c; Pi
substrate strictly prefers its absence.

---

## Part B — Dashboard tagline provenance

### B1. What computed "+26% / 39% DD / 34% knife"

- **Source file:** `algo/reports/sprint_v15_revised_6b_shadow.json`
  (generated 2026-04-20 in this session by
  `algo/diagnostics/sprint_v15_revised_6b_shadow.py`).
- **Replay parameters:** Kaggle ETH/USDT last 60 days. Params: SL=50,
  TP=150, knife=100, ext=None (v2 refit). E3 ON, timeout_trail ON,
  F3c OFF, leverage 5×, entropy-weighted sizing OFF.
- **Cell label in report:** `lev5x_ewoff_e3on` (revised 6b variant).

### B2. Substrate confirmation

**Confirmed: Kaggle-only.** Pi 21-day IS was NOT folded into the tagline
number. The tagline picks the more pessimistic of the two substrates
(Kaggle OOS +26% vs Pi IS +178%) without disclosing that. That's a
presentation problem — see Part F2.

---

## Part C — Shadow expectation buckets

### C1. Current `state/shadow_expectation.json` (no F3c)

| Bucket | n | mean (bps) | std (bps) | Fallback level |
|---|---:|---:|---:|---|
| long_europe     |  6 |  +38.6 | 128.2 | primary |
| long_asia       |  6 |  +36.5 | 123.2 | primary |
| long_americas   |  4 | +116.2 | 122.7 | **FALLBACK (n<5)** — uses `long` direction-only (n=16, mean +57.2, std 121.6) |
| short_europe    | 12 | +119.2 |  86.3 | primary |
| short_americas  |  8 |  +21.9 | **145.2** | primary — std flagged, see below |
| short_asia      |  8 |  +42.9 |  95.3 | primary |

**Flags:**

- `long_americas` has n=4 → falls back to direction-only.
- `short_americas` has std 145.2 bps on n=8. Max is +345.67 bps (one trade
  hit the trail_after + extended run), min is −50. The +345.67 outlier is
  ~2.3σ above the mean and drags the std up. Not a lone-trade artifact
  (n=8 is reasonable); but the mean here (+21.9) is sensitive — a single
  live drawdown in this bucket will z-score large against such a wide
  distribution, which reduces valve sensitivity in this bucket.
- Other buckets have std in the 86–128 bps range, which is consistent
  with F3c-off widening-the-distribution behavior (F3c was variance-
  narrowing on Pi; removing it lets tails reach farther).

### C2. v1 (F3c ON) vs current (no F3c) — bucket shift

Per-bucket mean shift (current minus v1):

| Bucket | v1 n | cur n | v1 mean (bps) | cur mean (bps) | Mean shift |
|---|---:|---:|---:|---:|---:|
| long_europe    |  6 |  6 |  +37.2 |  +38.6 |  +1.4 |
| long_asia      |  3 |  6 | +103.0 |  +36.5 | **−66.6** |
| long_americas  |  3 |  4 |  +67.7 | +116.2 | **+48.5** |
| short_europe   |  6 | 12 |  +86.8 | +119.2 | **+32.4** |
| short_americas |  4 |  8 |   −5.0 |  +21.9 | **+27.0** |
| short_asia     |  4 |  8 |  +33.0 |  +42.9 |  +9.9 |

**Direction of shifts:** 5 of 6 buckets improve mean when F3c is removed.
The lone regression is `long_asia` (−66.6 bps). But v1 `long_asia` had
only 3 samples (below fallback threshold anyway), so the shift is largely
a sampling-noise vs larger-sample comparison, not a clean apples-to-apples.

**Direction-only fallback shifts:**
- long: v1 n=12 mean +61.25 → cur n=16 mean +57.18 (−4.07 bps)
- short: v1 n=14 mean +45.22 → cur n=28 mean +69.64 (**+24.42 bps**)

Short side got MUCH better without F3c. This is consistent with sprint
v1.5 Step 3 finding that F3c's SHORT-side block rate on Kraken-native
was 71.8% — F3c was over-pruning shorts.

---

## Part D — 6a observer data

### D1. Days running

6a deployed 2026-04-20 01:26:17 +0300 (commit `7c8d67f`). Current local
time stamps on tonight's commits are 20:58 on 2026-04-20, so 6a has been
running **~19.5 hours, i.e. <1 day**. We are nowhere near the 3–7 day
window the dashboard advertises. **Too early for meaningful observer
statistics.**

### D2. `state/daily_filter_audit.jsonl` status

**Not present on the dev machine.** `algo/state/` directory contents:

```
multi_trader_state.json        (1.3 KB, last modified 2026-04-13)
shadow_expectation.json        (2.9 KB, today, no-F3c)
shadow_expectation.v1.json     (2.9 KB, today, preserved F3c-ON copy)
trade_charts.json              (10 KB, 2026-04-19)
trade_history.jsonl            (2.5 KB, 2026-04-15)
trade_history_pi_latest.jsonl  (2.5 KB, 2026-04-15)
transformer_direction.pt       (819 KB, 2026-04-17)
```

`algo/state/` is in `.gitignore`. The audit writer runs on the **Pi**,
not on this dev host, so its output is remote. To answer your D2
questions concretely I need either (a) `scp` from the Pi, or (b) access
to the Pi filesystem. Neither has been done in this session.

Candidate-bar signal rate, imbalance distribution, and writer errors —
all require the Pi file. **Deferred until we pull the file.**

### D3. Live trades during 6a

Local `trade_history.jsonl` last modified **2026-04-15**, i.e. before
6a deploy. No dev-side evidence of any live trade in the 6a window.
Again, authoritative record lives on the Pi. **Deferred.**

Pre-sprint trade rate context: 17 trades over 2026-04-12 through
2026-04-19 ≈ 2 trades/day. A <1-day observer window expects 0–3 trades
as normal. The fact that we haven't pulled any from the Pi doesn't mean
none have happened — just that we haven't looked.

---

## Part E — Infrastructure confirmations

### E1. Pi L2 collector status

**Not running.** The collector is the #1 priority item in the "Open
questions for next sprint" section of `docs/sprint_v1_findings.md`
(promoted this session) — that listing itself is the record that the
work has not been started. No code for a standalone L2-archiving
collector exists in the repo. The Pi bot's in-memory 30-min engine
window is still the only place L2 is held.

**When could it be started:** the collector could be started whenever
you decide. Rough scope: add a second WebSocket subscription
(already-subscribed `book` stream plus a periodic `dump-to-file` every
N seconds), write to `data/kraken_futures_l2_archive/{date}_{hour}.jsonl.gz`
on the Pi, cron-rotate. Estimate: 2–4 hours of work to stand up;
archive useful for sprint v2 after 30+ days of forward run.

### E2. `state/live_drift_monitor.json` status

**Does not exist** on the dev machine (deleted in the shadow-regen
commit `3740613`). No z-scores recorded yet on the dev side. Same
caveat as D2: authoritative file lives on the Pi. If the Pi has been
accumulating z-scores since 6a deploy, those are against the v1 shadow
(F3c-ON) expectation and should be discarded before 6b. Since revised
6b needs a fresh window anyway, I recommend deleting the Pi's
`live_drift_monitor.json` at the same time we push the new
`shadow_expectation.json` to the Pi.

### E3. `state/safety_valve.json` status

**Does not exist** on the dev machine. Valve has not tripped locally.
Dev machine is not the valve's target anyway — it would live on the
Pi. Remote status: not verified, but flags were default-False during 6a
so the valve's condition (sum of last 10 z-scores < −6.32 against
expected) cannot have been checked because no trades have been
scored against any expectation (expectation was stale / missing on
Pi). **No action needed, but re-verify on Pi when we push the new
expectation.**

---

## Part F — Honest self-assessment

### F1. Cadence on the session's commits (since 6a deploy)

Five commits landed this session: `897de11`, `ebc5ce5`, `4ac1934`,
`3f2c5da`, `3740613`.

| Commit | Cadence | Assessment |
|---|---|---|
| 897de11 sprint v1.5 closed RED | **Same as Phases 1-4** | Ran all 5 steps per your authorization, paused for your call, did not act on my own decision. ✓ |
| ebc5ce5 dashboard v1.5 RED (initial) | **Same** | Executed your 7-point dashboard spec literally. ✓ |
| 4ac1934 6b config template with F3c paused | **Same** | Executed your commenting-out spec literally. ✓ |
| 3f2c5da dashboard v1.5 RED (revised) | **Cadence slipped** | I computed the +26% number from ONE substrate (Kaggle OOS) without verifying it against the Pi-IS equivalent, then pushed it to the user-facing tagline. Pi-IS for the same cell is +178%. I should have paused to ask "which substrate do you want on the card" — or put both. Instead I picked the worse number and presented it as the promoted expectation. |
| 3740613 shadow regen no-F3c | **Cadence slipped** | Overwrote `shadow_expectation.json` immediately on first successful regen. Did not compare the new bucket stats to the old before overwriting. Did not notice the SHORT bucket mean shifting +24 bps (direction fallback) — which is a genuine signal that F3c was over-pruning shorts and which we should have surfaced in the commit message or the decision doc. |

**Summary:** two of the five commits were executed with lower verify
cadence than the earlier phases. Not disastrous (the actions were
reversible, not deployed), but worth flagging.

### F2. Commits I'd redo differently now

1. **`3f2c5da` dashboard tagline.** Would show two numbers, not one:
   `Shadow OOS: Pi 21d IS +178% / DD 8% · Kaggle 60d OOS +26% / DD 39%
    (substrate spread reflects F3c removal; track Pi once native-L2
    collector has 30+ days).` Honest, both numbers present, context
   for the spread.

2. **`3740613` shadow preservation filename.** Your instruction asked
   for `shadow_expectation.v1_with_f3c.json`. I named it
   `shadow_expectation.v1.json`. Functionally fine (no collision), but
   the explicit `_with_f3c` suffix would survive future additional-
   version accumulation better. I'd rename it.

3. **`897de11` sprint v1.5 decision doc.** I reported F3c RED on
   Kraken-native OOS but did NOT cross-check against Pi-IS at the
   same time. Today's Pi-IS replay (A2 above) shows F3c was ALSO
   net-negative on Pi IS when measured at the phase-4-equivalent cell.
   That result would have strengthened the RED verdict AND raised an
   earlier-sprint concern: phase 2's Pi F3c approval may have been
   an artifact of a different parameter set or different trade-engine
   config. I'd append an "A+B confirmation" section to the decision
   doc: "On Pi IS at revised-6b params (SL=50/TP=200/knife=50/ext=100,
   timeout_trail+E3+5x), removing F3c improves ret by +117pp, DD by
   −4.4pp, knife rate by −6.9pp. F3c failure is not specific to
   Kraken-native OOS — it reproduces on Pi IS too, just not at the
   phase-2 parameter set."

4. **Lack of explicit "does this substrate disagree with that one"
   protocol.** The session would have benefited from a standing rule
   that whenever we change a promoted flag, we replay BOTH substrates
   (Pi + Kaggle) at the exact same parameter config before we update
   any shadow expectation. I skipped the Pi half on `3740613`; user-
   driven discovery (this audit) caught it.

---

## Outstanding items for the next planning conversation

1. Decide whether the dashboard tagline should show Pi-IS (+178%),
   Kaggle-OOS (+26%), or both. My recommendation: both, with the
   substrate spread called out explicitly.
2. Decide whether to rename `shadow_expectation.v1.json` →
   `shadow_expectation.v1_with_f3c.json` for future-proofing.
3. Pull `state/daily_filter_audit.jsonl`, `state/live_drift_monitor.json`,
   and `state/trade_history.jsonl` from the Pi so we can actually
   answer D2/D3 with data.
4. Decide timing for standing up the Pi L2 collector (#1 priority for
   sprint v2; sooner = more forward archive by the time we re-test F3c
   or test any new filter).
5. Decide whether the Pi-IS finding (F3c net-negative at the revised-6b
   parameter set) warrants a one-line amendment to
   `algo/reports/sprint_v15_decision.md` before we move on, or whether
   to capture it in the sprint v2 opening memo.

No changes made. Paused after writing.
